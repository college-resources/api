type Meal {
  time: String
  menu: String
}

input MealInput {
  time: String
  menu: String
}

type Day {
  meals: [Meal!]!
}

type Week {
  days: [Day!]!
}

input DayInput {
  meals: [MealInput!]!
}

type Feeding {
  _id: ID!
  weeks: [Week!]!
  startsFrom: String!
}

input FeedingInput {
  days: [DayInput!]!
  startsFrom: String!
}
