type Meal {
  timeStart: String
  timeEnd: String
  menu: String
}

input MealInput {
  timeStart: String
  timeEnd: String
  menu: String
}

type Day {
  meals: [Meal!]!
}

type Week {
  days: [Day!]!
}

input DayInput {
  meals: [MealInput!]!
}

input WeekInput {
  days: [DayInput!]!
}

type Feeding {
  _id: ID!
  weeks: [Week!]!
  startsFrom: String!
}

input FeedingInput {
  weeks: [WeekInput!]!
  startsFrom: String!
}
