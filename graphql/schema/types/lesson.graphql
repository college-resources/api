# import Image, ImageInput from "./image.graphql"
# import User from "./user.graphql"

type Lesson {
  _id: ID!
  name: String!
  department: String!
  semester: Int!
  creator: User!
}

type LessonNote {
  _id: ID!
  images: [Image!]!
  hypertexts: [String!]!
  lesson: Lesson!
  creator: User!
}

input LessonInput {
  name: String!
  department: String!
  semester: Int!
}

input LessonNoteInput {
  images: [ImageInput!]
  hypertexts: [String!]
  lesson: ID!
}
